This directory is intentionally reduced in the public repository.

The original `models_v3` package contained private research code, trained heads,
and proprietary model implementations. It has been removed from the public
release while the main model orchestration layer remains available under
`core/models/`.
