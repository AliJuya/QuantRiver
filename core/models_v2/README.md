This directory is intentionally reduced in the public repository.

The original `models_v2` package contained private research code and training-time
components. It has been removed from the public release to avoid exposing
proprietary model implementations while keeping the repository structure
understandable.
